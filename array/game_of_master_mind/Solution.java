public class Solution {
    public int[] calculate (String guess, String slots) {
        int[] hits = new int[2];
        // hits[0] is the true hits;
        // hits[1] is the pseudo hits.
        for (int i = 0; i < guess.length(); i++) {
            
        }
    }
}